# NNN - Short Title

**Status:** Todo · **Depends on:** <ticket(s) or none> · **Spec:** [spec.md](../spec.md#section)

**Summary** - 1-3 sentences describing what this delivers and why. Optionally:
_As a `<role>`, I want `<x>` so that `<y>`._

**Acceptance criteria**

- [ ] Given `<context>`, when `<action>`, then `<observable outcome>`.
- [ ] Given `<context>`, when `<invalid action>`, then `<rejection or error>`.
- [ ] Given `<existing behavior>`, when this ships, then `<behavior>` is unchanged.

**Tasks**

- [ ] Implementation step.
- [ ] Implementation step.
- [ ] Tests.
- [ ] Seed data, only when applicable.

**Notes** (optional)

- Key decision or gotcha with a link to the relevant spec section.
- Explicit deferral only when a reader might reasonably assume it is included.
