## Parent

Epic: #NN

## What to build

1-3 sentences describing the end-to-end behavior this ticket makes work, from
the user's perspective, rather than a layer-by-layer implementation list.
Optionally: _As a `<role>`, I want `<x>` so that `<y>`._

## Acceptance criteria

- [ ] Given `<context>`, when `<action>`, then `<observable outcome>`.
- [ ] Given `<context>`, when `<invalid action>`, then `<rejection or error>`.
- [ ] Given `<existing behavior>`, when this ships, then `<behavior>` is unchanged.

## Tasks

- [ ] Implementation step.
- [ ] Implementation step.
- [ ] Tests.
- [ ] Seed data, only when applicable.

## Blocked by

- #NN, or "None - can start immediately".

## Notes

- Key decision or gotcha with a link to the relevant spec section.
- Explicit deferral only when a reader might reasonably assume it is included.

## Spec

[<Section>](https://github.com/proofplane/proofplane/blob/main/docs/epics/<epic-slug>/spec.md#section)
