# NNN — Short Title

**Status:** Todo · **Depends on:** <ticket(s) or none> · **Spec:** [spec.md](../spec.md#section)

**Summary** — 1–3 sentences: what this delivers and why. Optionally: _As a `<role>`, I want `<x>` so that `<y>`._

**Acceptance criteria**

- [ ] Given `<context>`, when `<action>`, then `<observable outcome>`.
- [ ] Given `<context>`, when `<invalid action>`, then `<rejection / error>`.
- [ ] Given `<existing behavior>`, when this ships, then `<X>` is unchanged.

**Tasks**

- [ ] Implementation step.
- [ ] Implementation step.
- [ ] Tests.
- [ ] Seed data (optional - only if it makes sense)

**Notes** (optional)

- Key decision or gotcha, pointing at the spec for detail. Don't restate the spec.
- (Only if a reader might assume otherwise) "X is deferred to NNN".
