## Parent

Epic: #NN

## What to build

1–3 sentences: the end-to-end behavior this ticket makes work, from the user's
perspective — not a layer-by-layer implementation list. Optionally: _As a
`<role>`, I want `<x>` so that `<y>`._

## Acceptance criteria

- [ ] Given `<context>`, when `<action>`, then `<observable outcome>`.
- [ ] Given `<context>`, when `<invalid action>`, then `<rejection / error>`.
- [ ] Given `<existing behavior>`, when this ships, then `<X>` is unchanged.

## Tasks

- [ ] Implementation step.
- [ ] Implementation step.
- [ ] Tests.
- [ ] Seed data (optional — only if it makes sense).

## Blocked by

- #NN — or "None — can start immediately".

## Notes

- Key decision or gotcha, pointing at the spec for detail. Don't restate the spec.
- (Only if a reader might assume otherwise) "X is deferred to #NN".

## Spec

[<Section>](https://github.com/proofplane/proofplane/blob/main/docs/epics/<epic-slug>/spec.md#section)
